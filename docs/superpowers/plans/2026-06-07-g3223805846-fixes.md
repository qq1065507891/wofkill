# g_3223805846 Post-Mortem Fixes Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 修复 game_g_3223805846 分析中暴露的 24 个提示词 / 工程缺陷，让真预言家不再被错杀、狼队不再自爆信息、FALLBACK 不再投出关键错票。

**Architecture:** 两条 worktree 分支并行 — `fix10-prompt-g3223805846` 修 9 个 prompt 缺陷（Batches A1-A4），`fix11-engineering-g3223805846` 修 9 个工程缺陷（Batches B1-B3），各跑 TDD 后合回 master。每条 commit 独立可回滚。

**Tech Stack:** Python 3.11, Pydantic v2, pytest, LangGraph, 现有 `werewolf_agent` 包结构。

**Spec source:** `game_g_3223805846.json` (10960 行) + 本会话前面的分析（已存档到 memory `project-context.md`）。

---

## 文件变更地图

| 类别 | 文件 | 变更类型 | 涉及 Batch |
|---|---|---|---|
| 提示词 | `werewolf_agent/runtime/directives/wolf.py` | 改 fake_seer 段、加 fake_seer_consistency | A1, A2, A4 |
| 提示词 | `werewolf_agent/runtime/directives/seer.py` | 加后位硬约束段 | A3 |
| 提示词 | `werewolf_agent/runtime/directives/witch.py` | 加公开来源校验 | A5 |
| 提示词 | `werewolf_agent/runtime/directives/hunter.py` | 加开枪前孤证兜底 | A6 |
| 提示词 | `werewolf_agent/runtime/directives/hybrid.py` | 跟随/反向投票规则 | A7 |
| 提示词 | `werewolf_agent/agents/prompt_builder.py` | 私视角表述清洗 + action_type 字段名强化 | A8, B4 |
| 提示词 | `werewolf_agent/runtime/directives/_shared.py` | 无警长归票 hint | A9 |
| 工程 | `werewolf_agent/agents/player.py` (`_fallback_action`) | vote fallback target 选择 + 死人过滤 | B1, B2, B6 |
| 工程 | `werewolf_agent/agents/output_parser.py` | typo tolerance | B5 |
| 工程 | `werewolf_agent/agents/schemas.py` | `total_retry_count_until_success` 字段 | C1 |
| 工程 | `werewolf_agent/runtime/nodes/_shared.py` (`_planned_wolf_kill`) | primary 不可达校验 | B7 |
| 工程 | `werewolf_agent/runtime/agent_adapter.py` | seer PK 段非空保护 | B3 |
| 测试 | `tests/runtime/test_strategy_directives.py` | 加 A1-A9 测试 | A1-A9 |
| 测试 | `tests/runtime/test_wolf_flow.py` | 加 A2 实时神职列表测试 | A2 |
| 测试 | `tests/agents/test_player_agent.py` | 加 B1-B6 测试 | B1-B6 |
| 测试 | `tests/agents/test_output_parser.py` | 加 B5 typo tolerance 测试 | B5 |
| 测试 | `tests/agents/test_schemas.py` | 加 C1 测试 | C1 |

---

# Branch 1: `fix10-prompt-g3223805846`

## Batch A1: 狼队公开话术不暴露刀口

### Task A1.1: 写 wolf 公开话术黑名单测试

**Files:**
- Modify: `tests/runtime/test_strategy_directives.py` (新增 TestWolfDirectiveNoLeakKill class)

- [ ] **Step 1: 写失败测试**

在 `tests/runtime/test_strategy_directives.py` 末尾加：

```python
class TestWolfDirectiveNoLeakKill:
    """P0-G3223805846-1: wolf 公开话术禁止出现具体刀口 ID 列表。"""

    def test_fake_seer_directive_excludes_kill_target_examples(self):
        from werewolf_agent.runtime.directives.wolf import _WOLF_ROLE_STRATEGY
        text = _WOLF_ROLE_STRATEGY["fake_seer"]
        # 禁止"刀口是 p01/p07"这种列举式表达
        for forbidden in ("刀口是", "我们狼人刀了", "我们 N1 刀", "我们 N2 刀",
                          "刀了 p0", "刀 p0", "杀了 p0"):
            assert forbidden not in text, (
                f"fake_seer directive leaks kill targets: '{forbidden}' found in {text!r}"
            )

    def test_wolf_universal_rules_excludes_kill_target_examples(self):
        from werewolf_agent.runtime.directives.wolf import (
            _build_wolf_universal_rules,
        ) if False else None  # 直接测最终 prompt
        # 通过 build_wolf_directive 拿完整 prompt
        from werewolf_agent.runtime.directives.wolf import build_wolf_directive
        from werewolf_agent.core.models import GameState
        gs = GameState(players={}, day_number=1, night_number=1)
        d = build_wolf_directive(gs, "p01", wolf_team_plan={"fake_seer": "p01"})
        full = " ".join(str(v) for v in d.values())
        for forbidden in ("刀口是 p0", "我们 N1 刀", "我们 N2 刀", "我们狼人刀"):
            assert forbidden not in full, f"wolf directive leaks: {forbidden}"
```

- [ ] **Step 2: 跑测试确认失败**

Run: `pytest tests/runtime/test_strategy_directives.py::TestWolfDirectiveNoLeakKill -v`
Expected: FAIL（当前 `_WOLF_ROLE_STRATEGY["fake_seer"]` 含"你'验出'的好人可以是你的队友"等，但还没"刀口是"具体字段——会过；但 `test_wolf_universal_rules` 会暴露问题）

---

### Task A1.2: 改 `_WOLF_ROLE_STRATEGY["fake_seer"]` 话术

**Files:**
- Modify: `werewolf_agent/runtime/directives/wolf.py:13-23`

- [ ] **Step 1: 改 fake_seer 话术**

把 `wolf.py:13-23` 替换为：

```python
    "fake_seer": (
        "你是悍跳狼（假预言家）。白天发言策略：\n"
        "1) 跳预言家，报出你的'验人结果'。必须像真预言家一样有验人逻辑链\n"
        "2) **关键规则**：预言家一夜只能查验 1 人，绝对不能声称同一夜查验多人\n"
        "3) 如果场上有真预言家跳了，你必须对跳——质疑真预言家的验人逻辑和警徽流\n"
        "4) 报验人的标准格式：'我在第X夜验了[玩家]，结果是[好人/狼人]'\n"
        "5) 你可以报一个好人做金水来拉拢，或者报一个好人查杀来推人\n"
        "6) 不要暴露你的队友——你'验出'的好人可以是你的队友（假金水）\n"
        "7) 如果真预言家验出了狼（你的队友），你必须质疑该验人结果的可信度\n"
        "8) 不要慌张——对跳预言家是正常游戏行为，保持自信和逻辑连贯\n"
        "9) **严禁在公开发言/投票理由中提及狼队真实刀口**：禁止使用'刀口是 p0X'、"
        "'我们 N1 刀了 p0X'、'狼队根本没刀 p0X'这类列举真实刀口的表述。"
        "质疑真预言家时只能使用'时间线对不上'、'逻辑说不通'、'你的查验和实际死亡不符'"
        "等模糊话术，**永远不要透露你作为狼人知道的具体刀口 ID 列表**。"
        "若必须提到死亡名单，只能引用'昨夜死亡 X'的公开广播文字。"
    ),
```

- [ ] **Step 2: 跑测试**

Run: `pytest tests/runtime/test_strategy_directives.py::TestWolfDirectiveNoLeakKill -v`
Expected: PASS

- [ ] **Step 3: 跑相关测试**

Run: `pytest tests/runtime/test_strategy_directives.py tests/runtime/test_wolf_flow.py -q`
Expected: all pass

- [ ] **Step 4: Commit**

```bash
git add werewolf_agent/runtime/directives/wolf.py tests/runtime/test_strategy_directives.py
git commit -m "fix(wolf): fake-seer 公开话术禁止列举真实刀口 ID (g_3223805846-A1)"
```

---

## Batch A2: 狼 prompt 拉取实时"已跳神职"列表（解 N2 幻觉）

### Task A2.1: 写测试

**Files:**
- Modify: `tests/runtime/test_wolf_flow.py` (新增 TestWolfDirectiveLiveSeerClaimants)

- [ ] **Step 1: 写失败测试**

```python
class TestWolfDirectiveLiveSeerClaimants:
    """P0-G3223805846-2: 狼 prompt 中"已跳预言家"必须基于 day_speech 实时事件。"""

    def test_wolf_directive_lists_actual_seer_claimants_not_hallucinated(self):
        from werewolf_agent.runtime.directives.wolf import build_wolf_directive
        from werewolf_agent.core.models import GameState, PlayerState

        # 构造 GS：p01 真预言家公开跳，p02 悍跳，p07 是村民但被狼记忆错误为"跳了"
        gs = GameState(
            players={
                "p01": PlayerState(role="werewolf", alive=True),  # 这里把 p01 设狼
                "p02": PlayerState(role="werewolf", alive=True),
                "p07": PlayerState(role="villager", alive=True),
            },
            day_number=1,
            night_number=1,
            events=[
                # p01 真预言家发言：报 p02 是狼
                {"type": "speech", "payload": {
                    "speaker": "p01", "text": "我是预言家，第 1 夜验 p02 是狼人。"
                }},
                # p02 悍跳发言
                {"type": "speech", "payload": {
                    "speaker": "p02", "text": "我是预言家，第 1 夜验 p01 是好人（金水）。"
                }},
                # p07 村民发言（没跳预言家）
                {"type": "speech", "payload": {
                    "speaker": "p07", "text": "我站边 p01，p02 是悍跳。"
                }},
            ],
        )
        d = build_wolf_directive(gs, "p01", wolf_team_plan=None)
        full = " ".join(str(v) for v in d.values())
        # 狼 prompt 应明确提到"场上已跳预言家是 p01/p02"，不应出现"p07 跳预言家"
        assert "p07" not in full or "p07 没跳" in full or "村民" in full, (
            f"wolf directive references p07 as seer claimant: {full!r}"
        )
        # 应正确识别 p01 和 p02
        assert "p01" in full or "p02" in full
```

- [ ] **Step 2: 跑测试确认失败**

Run: `pytest tests/runtime/test_wolf_flow.py::TestWolfDirectiveLiveSeerClaimants -v`
Expected: FAIL（当前 build_wolf_directive 没有 live seer claimant block）

---

### Task A2.2: 在 `build_wolf_directive` 加 live seer claimants 段

**Files:**
- Modify: `werewolf_agent/runtime/directives/wolf.py` (在 line 73 之后)

- [ ] **Step 1: 加函数 + 注入**

在 `wolf.py` 顶部 import 区加：

```python
from werewolf_agent.runtime.strategy import (
    public_seer_claimants as _public_seer_claimants,
)
```

在 `build_wolf_directive` 函数开头（line 73 附近）`parts = {}` 之后插入：

```python
    # P0-G3223805846-2: live seer claimants from day_speech events.
    # This avoids the N2 hallucination where wolves cited p07 as
    # the seer claimant (p07 was actually a villager who never
    # claimed seer).  Source from public events so the prompt
    # reflects what actually happened on day, not stale memory.
    claimants = _public_seer_claimants(gs)
    if claimants:
        parts["wolf_live_seer_claimants"] = (
            "【实时场上信息】以下玩家已在白天公开场跳预言家："
            + ", ".join(sorted(claimants))
            + "。\n讨论/制定狼队策略时，**只参考以上已公开声明的玩家**。"
            "不要凭印象或记忆把'看起来像预言家'的玩家列入。"
            "如果列表为空，说明当前没有玩家公开跳预言家。"
        )
```

- [ ] **Step 2: 跑测试**

Run: `pytest tests/runtime/test_wolf_flow.py::TestWolfDirectiveLiveSeerClaimants -v`
Expected: PASS

- [ ] **Step 3: 跑相关测试**

Run: `pytest tests/runtime/test_wolf_flow.py tests/runtime/test_strategy_directives.py -q`
Expected: all pass

- [ ] **Step 4: Commit**

```bash
git add werewolf_agent/runtime/directives/wolf.py tests/runtime/test_wolf_flow.py
git commit -m "fix(wolf): 狼 prompt 注入实时'已跳预言家'列表 (g_3223805846-A2)"
```

---

## Batch A3: 预言家后位硬约束

### Task A3.1: 写测试

**Files:**
- Modify: `tests/runtime/test_strategy_directives.py` (新增 TestSeerDirectiveLatePosition)

- [ ] **Step 1: 写失败测试**

```python
class TestSeerDirectiveLatePosition:
    """P0-G3223805846-3: 预言家排到发言顺序后段必须强制第 1 句亮身份。"""

    def test_seer_in_late_position_must_jump_immediately(self):
        from werewolf_agent.runtime.directives.seer import build_seer_directive
        from werewolf_agent.core.models import GameState, PlayerState

        # 构造：12 人，p01 真预言家当前排在 11/12 位
        alive = {f"p{i:02d}": PlayerState(role="villager", alive=True) for i in range(1, 13)}
        alive["p01"] = PlayerState(role="seer", alive=True)
        gs = GameState(players=alive, day_number=1, night_number=1)
        # 模拟当前发言序
        speech_order = [f"p{i:02d}" for i in [8, 11, 2, 12, 6, 4, 10, 5, 7, 3, 1, 9]]
        d = build_seer_directive(gs, "p01", speech_order=speech_order)
        directive = d.get("seer_speech_directive", "")
        assert "后段" in directive or "立即" in directive or "第 1 句" in directive, (
            f"late-position seer missing jump-immediately rule: {directive!r}"
        )

    def test_seer_in_early_position_no_jump_immediately_rule(self):
        from werewolf_agent.runtime.directives.seer import build_seer_directive
        from werewolf_agent.core.models import GameState, PlayerState
        alive = {f"p{i:02d}": PlayerState(role="villager", alive=True) for i in range(1, 13)}
        alive["p01"] = PlayerState(role="seer", alive=True)
        gs = GameState(players=alive, day_number=1, night_number=1)
        speech_order = ["p01", "p02", "p03", "p04", "p05", "p06",
                        "p07", "p08", "p09", "p10", "p11", "p12"]
        d = build_seer_directive(gs, "p01", speech_order=speech_order)
        directive = d.get("seer_speech_directive", "")
        # 前 1/3 不强制立即跳
        assert "后段" not in directive, f"early seer incorrectly tagged as late: {directive!r}"
```

注：当前 `build_seer_directive(gs, seer_id)` 签名无 `speech_order`，需要改签名。

- [ ] **Step 2: 跑测试确认失败**

Run: `pytest tests/runtime/test_strategy_directives.py::TestSeerDirectiveLatePosition -v`
Expected: FAIL（两个测试都因签名不匹配 / 缺逻辑失败）

---

### Task A3.2: 改 `build_seer_directive` 签名 + 加后位约束

**Files:**
- Modify: `werewolf_agent/runtime/directives/seer.py:10-15`

- [ ] **Step 1: 改签名**

`seer.py:10-15`：

```python
def build_seer_directive(
    gs: GameState,
    seer_id: str,
    speech_order: list[str] | None = None,
) -> dict[str, Any]:
```

- [ ] **Step 2: 在 `reporting_parts` 末尾追加位置段**

在 `seer.py` `reporting_parts.append("\n报验人的标准格式...")` 之后追加：

```python
    # P0-G3223805846-3: late-speech-position seer must jump immediately
    if speech_order and seer_id in speech_order:
        pos = speech_order.index(seer_id)
        total = len(speech_order)
        if total > 0 and pos >= total * 0.5:
            reporting_parts.append(
                f"\n【后位硬约束】你当前排在发言顺序第 {pos + 1}/{total} 位（后段）。"
                "**必须在本次发言的第 1 句就公开声明预言家身份 + 报出首夜查杀**，"
                "不能等别人先跳。前段玩家会先你发言完毕，他们的发言可能已经形成错误共识，"
                "越晚亮身份被错杀的概率越高。"
                "具体格式：'我是预言家，第 1 夜验了 pXX 是 [好人/狼人]'——"
                "首句先报这两件事，再展开分析。"
            )
```

- [ ] **Step 3: 找调用方并更新签名**

Run: `grep -rn "build_seer_directive" werewolf_agent/`

预期调用方在 `runtime/agent_adapter.py`。把调用处从：

```python
build_seer_directive(gs, seer_id)
```

改为：

```python
build_seer_directive(gs, seer_id, speech_order=current_speech_order)
```

`current_speech_order` 应从 `gs` 当前 day_speech 的生成节点取（一般在 `graph.py` 或 `agent_adapter.py` 的 `agent_day_speech` 调用现场）。具体取法可从 `gs._current_day_speech_order` 字段或 `gs.events` 反查当日所有 `speaker_turn` 事件。

- [ ] **Step 4: 跑测试**

Run: `pytest tests/runtime/test_strategy_directives.py::TestSeerDirectiveLatePosition -v`
Expected: PASS

- [ ] **Step 5: 跑全量 regression**

Run: `pytest tests/runtime/ tests/agents/ tests/rules/ -q`
Expected: all pass

- [ ] **Step 6: Commit**

```bash
git add werewolf_agent/runtime/directives/seer.py werewolf_agent/runtime/agent_adapter.py tests/runtime/test_strategy_directives.py
git commit -m "fix(seer): 后位发言必须第 1 句亮身份 + 报查杀 (g_3223805846-A3)"
```

---

## Batch A4: 狼队内部 fake-seer 话术统一（防自相矛盾）

### Task A4.1: 写测试

**Files:**
- Modify: `tests/runtime/test_strategy_directives.py` (新增 TestWolfFakeSeerConsistency)

- [ ] **Step 1: 写失败测试**

```python
class TestWolfFakeSeerConsistency:
    """P0-G3223805846-4: 狼 fake_seer 启用时所有狼的 prompt 必须共享话术。"""

    def test_fake_seer_prompt_includes_consistency_clause(self):
        from werewolf_agent.runtime.directives.wolf import _WOLF_ROLE_STRATEGY
        # fake_seer / pusher / hooker / deep_cover / unassigned 都应包含一致性条款
        for role in ("fake_seer", "pusher", "hooker", "deep_cover", "unassigned"):
            text = _WOLF_ROLE_STRATEGY.get(role, "")
            # 各角色策略里应有"和悍跳狼话术保持一致"的提示
            assert "话术一致" in text or "保持一致" in text or "对跳的查验" in text, (
                f"role {role} missing fake-seer consistency clause"
            )
```

- [ ] **Step 2: 跑测试确认失败**

Run: `pytest tests/runtime/test_strategy_directives.py::TestWolfFakeSeerConsistency -v`
Expected: FAIL

---

### Task A4.2: 在各角色策略加一致性条款

**Files:**
- Modify: `werewolf_agent/runtime/directives/wolf.py:13-58`

- [ ] **Step 1: pusher 加一致性**

`wolf.py:24-32` 末尾加：

```python
    "pusher": (
        "你是冲锋狼。白天发言策略：\n"
        "1) 你是狼队的攻击手——主动带节奏、推人、制造混乱\n"
        "2) 集中火力推团队的白天目标（day_push_target），用具体的'证据'指控\n"
        "3) 可以质疑预言家的验人结果，为悍跳狼队友站台\n"
        "4) 发言要有攻击性但不要无脑——每个指控都需要'理由'\n"
        "5) 如果悍跳狼被质疑，你要主动为其辩护或转移话题\n"
        "6) 不要直接暴露和队友的配合关系——表现得像独立判断\n"
        "7) **fake_seer 话术一致**：当队友悍跳时，TA 报的金水/查杀/警徽流，"
        "你在公开场必须复用 TA 的口径——如果 TA 报'p01 是金水'，你就不要公开质疑 p01；"
        "如果 TA 报'p07 是查杀'，你就当好人对 p07 施压。**不要说出与 TA 矛盾的死亡名单或刀口**。"
    ),
```

- [ ] **Step 2: hooker 加一致性**

`wolf.py:33-41` 末尾加：

```python
    "hooker": (
        "你是倒钩狼。白天发言策略：\n"
        "1) 核心任务：获取好人信任。你的价值在于'被信任后的背叛'\n"
        "2) 可以轻踩队友（质疑悍跳狼的验人、指出冲锋狼的漏洞）来换取信任\n"
        "3) 踩队友时必须用独立逻辑——'我觉得X的验人时间线不对'而不是'他是狼'\n"
        "4) 投票时可以跟好人走（投队友）来加深信任\n"
        "5) 不要太早暴露——N1/D1尽量低调，D2+再开始'独立分析'\n"
        "6) 关键时刻（4-5人残局）你可以突然跳出来带节奏推好人\n"
        "7) **fake_seer 话术一致**：轻踩悍跳狼时**只质疑 TA 的验人逻辑**（'时间线对不上'），"
        "**不要说出与 TA 报出的查验结果矛盾的具体死亡名单**。你踩的是 TA 的'论证方式'，"
        "不是 TA 的'结论'。"
    ),
```

- [ ] **Step 3: deep_cover 加一致性**

`wolf.py:42-51` 末尾加：

```python
    "deep_cover": (
        "你是深水狼。白天发言策略：\n"
        "1) 核心任务：像普通村民一样存活到最后。完全隐藏身份\n"
        "2) 表现得像一个有分析能力的普通好人——参与讨论、表达站边、分析逻辑\n"
        "3) 不要太出色引人注目，也不要太沉默被怀疑\n"
        "4) 可以帮真预言家站边（如果真预言家已经暴露），增强你的好人面\n"
        "5) 不要主动为队友辩护——那会暴露你们的关系\n"
        "6) 如果队友被推，表现得'意外'并附和好人的推人逻辑\n"
        "7) 你的目标是活到最后阶段（3-4人残局），那时你的1票就能决定胜负\n"
        "8) **fake_seer 话术一致**：当悍跳狼存活时，TA 报的金水/查杀是你在公开场接受的事实，"
        "你不要在发言中说出'与 TA 报出的查验矛盾'的内容。"
    ),
```

- [ ] **Step 4: unassigned 加一致性**

`wolf.py:52-58` 末尾加：

```python
    "unassigned": (
        "你是狼人，但没有特定角色分工。白天发言策略：\n"
        "1) 表现得像一个普通好人——参与讨论、表达站边\n"
        "2) 不要暴露队友，不要暴露自己\n"
        "3) 观察场上局势，配合队友的节奏\n"
        "4) 投票时注意不要和队友完全一致\n"
        "5) **fake_seer 话术一致**：悍跳狼报的查验是你接受的事实，"
        "不要在发言中引用与 TA 矛盾的死亡名单或刀口信息。"
    ),
```

- [ ] **Step 5: 跑测试**

Run: `pytest tests/runtime/test_strategy_directives.py::TestWolfFakeSeerConsistency -v`
Expected: PASS

- [ ] **Step 6: Commit**

```bash
git add werewolf_agent/runtime/directives/wolf.py tests/runtime/test_strategy_directives.py
git commit -m "fix(wolf): 各角色策略加 fake_seer 话术一致条款 (g_3223805846-A4)"
```

---

## Batch A5: 女巫毒药公开依据校验

### Task A5.1: 写测试

**Files:**
- Modify: `tests/runtime/test_strategy_directives.py` (新增 TestWitchPoisonPublicSource)

- [ ] **Step 1: 写失败测试**

```python
class TestWitchPoisonPublicSource:
    """P0-G3223805846-5: 女巫毒药 reason 必须能追溯到公开事件。"""

    def test_witch_poison_directive_requires_public_evidence(self):
        from werewolf_agent.runtime.directives.witch import build_witch_directive
        from werewolf_agent.core.models import GameState, PlayerState

        # 构造：p03 女巫，p05 公开报"查杀 p06"（p05 是狼悍跳）
        alive = {f"p{i:02d}": PlayerState(role="villager", alive=True) for i in range(1, 13)}
        alive["p03"] = PlayerState(role="witch", alive=True)
        alive["p05"] = PlayerState(role="werewolf", alive=True)
        gs = GameState(
            players=alive, day_number=2, night_number=2,
            events=[
                {"type": "speech", "payload": {
                    "speaker": "p05", "text": "我是预言家，第 1 夜验 p06 是狼人。"
                }},
            ],
        )
        d = build_witch_directive(gs, "p03")
        directive = d.get("witch_speech_directive", "") + " ".join(
            str(v) for v in d.values() if v != d.get("witch_speech_directive", "")
        )
        # 必须有"毒药必须基于公开查杀/悍跳/fake seer 报出的具体 ID"的硬约束
        assert "公开" in directive and ("查杀" in directive or "悍跳" in directive), (
            f"witch poison directive missing public-source rule: {directive!r}"
        )
        # 必须禁止凭"看起来像狼"这种模糊理由
        assert "禁止" in directive or "不能" in directive, (
            f"witch poison directive missing prohibition: {directive!r}"
        )
```

- [ ] **Step 2: 跑测试确认失败**

Run: `pytest tests/runtime/test_strategy_directives.py::TestWitchPoisonPublicSource -v`
Expected: FAIL

---

### Task A5.2: 改 `build_witch_directive` 加公开依据段

**Files:**
- Modify: `werewolf_agent/runtime/directives/witch.py:29-37`

- [ ] **Step 1: 在 base_text 末尾加硬约束**

`witch.py:29-37` `base_text` 末尾追加：

```python
    base_text = (
        "你是女巫，你掌握的夜间信息（谁被刀、药水使用情况、救了谁、毒了谁）"
        "是你的核心优势。不要轻易暴露这些信息——一旦公开，狼人会知道你的"
        "药水状态并针对性调整策略。但在以下情况可以适度透露：\n"
        "1) 你即将死亡需要传递关键信息；\n"
        "2) 场上好人阵营信息严重不足，需要你站出来带队；\n"
        "3) 有人假冒女巫需要你自证身份。\n"
        "透露时也要衡量利弊，不要在第一天就全部交底。\n\n"
        "【毒药公开依据校验 P0-G3223805846-5】\n"
        "你的毒药目标必须能在 day_speech 公开事件里追溯到以下任一来源：\n"
        "- 已被预言家（含悍跳预言家）报为'查杀'的具体玩家 ID\n"
        "- 已被多人明确指控并形成证据链的具体玩家 ID\n"
        "- 已公开跳神职/悍跳且证据不足自洽的具体玩家 ID\n"
        "**禁止**仅凭'看起来像狼'、'语气不对'、'可能是神'这类模糊理由用毒。"
        "若你 reason 中引用的 player_id 实际是由别人报的查杀（如 p05 报'p06 是狼'），"
        "请在 reason 中明确写出'基于 p05 的查杀'以避免自己记错。\n"
        "【解药策略 P0-G3223805846-5】\n"
        "解药默认救当晚狼刀目标；若该目标已被多人质疑为悍跳/查杀狼，"
        "仍应救（解药救的是'今晚要死的人'，不是'好人'）。"
    )
```

- [ ] **Step 2: 跑测试**

Run: `pytest tests/runtime/test_strategy_directives.py::TestWitchPoisonPublicSource -v`
Expected: PASS

- [ ] **Step 3: Commit**

```bash
git add werewolf_agent/runtime/directives/witch.py tests/runtime/test_strategy_directives.py
git commit -m "fix(witch): 毒药/解药加公开依据硬约束 (g_3223805846-A5)"
```

---

## Batch A6: 猎人开枪前孤证兜底

### Task A6.1: 写测试

**Files:**
- Modify: `tests/runtime/test_strategy_directives.py` (新增 TestHunterShotEvidence)

- [ ] **Step 1: 写失败测试**

```python
class TestHunterShotEvidence:
    """P0-G3223805846-6: 猎人开枪前必须 ≥ 2 独立公开证据，否则倾向 no_action。"""

    def test_hunter_directive_requires_multi_evidence_for_shot(self):
        from werewolf_agent.runtime.directives.hunter import build_hunter_directive
        from werewolf_agent.core.models import GameState, PlayerState

        alive = {f"p{i:02d}": PlayerState(role="villager", alive=True) for i in range(1, 13)}
        alive["p09"] = PlayerState(role="hunter", alive=True)
        gs = GameState(players=alive, day_number=2, night_number=2)
        d = build_hunter_directive(gs, "p09")
        directive = d.get("hunter_speech_directive", "")
        # 必须有"≥ 2 处独立证据"硬约束
        assert ("2" in directive and ("证据" in directive or "独立" in directive)), (
            f"hunter shot directive missing multi-evidence rule: {directive!r}"
        )
        # 必须有"孤证时 no_action"兜底
        assert "no_action" in directive or "不开枪" in directive or "不开枪" in directive, (
            f"hunter directive missing no_action fallback: {directive!r}"
        )
```

- [ ] **Step 2: 跑测试确认失败**

Run: `pytest tests/runtime/test_strategy_directives.py::TestHunterShotEvidence -v`
Expected: FAIL

---

### Task A6.2: 改 `build_hunter_directive` 加孤证兜底

**Files:**
- Modify: `werewolf_agent/runtime/directives/hunter.py:39-47` 和 `48-57`

- [ ] **Step 1: 改 `identity_exposed` 分支**

`hunter.py:40-47` 替换为：

```python
    if identity_exposed:
        text = (
            "你是猎人，且你的身份已经公开。\n"
            "身份公开后的策略：\n"
            "1) 利用'我有枪'的威慑力，给狼人施加压力\n"
            "2) 明确表达你的怀疑和站边，让狼人忌惮开枪带走他们\n"
            "3) 不要虚张声势说你会带走某人——如果你被毒杀将无法开枪\n"
            "4) 如果预言家已死，你可以主动承担信息整理和归票的职责\n"
            "5) 【开枪前硬约束 P0-G3223805846-6】临死开枪必须基于"
            "**≥ 2 处独立公开证据**指向同一目标（预言家报查杀 + 至少 1 处"
            "公开站边/票型/发言矛盾），单点孤证时倾向 no_action。"
            "如果场上只剩 1-2 个疑似目标且证据都不足，宁可不开枪也不要乱开——"
            "乱开带走好人对好人阵营是灾难。"
        )
```

- [ ] **Step 2: 改非暴露分支**

`hunter.py:48-57` 替换为：

```python
    else:
        text = (
            "你是猎人，但你的身份尚未公开。\n"
            "猎人发言策略（核心：隐藏身份）：\n"
            "1) 不要暴露自己是猎人！狼人知道你是猎人后会避免刀你、改让女巫毒杀来禁枪\n"
            "2) 像普通村民一样发言，参与讨论、表达站边、分析逻辑\n"
            "3) 注意观察谁发言矛盾、站边模糊——这些是你未来可能的枪击目标\n"
            "4) 如果预言家验了你且报了金水，可以帮预言家站边增强好人阵营凝聚力\n"
            "5) 不需要刻意低调到完全沉默，正常参与讨论即可\n"
            "6) 【开枪前硬约束 P0-G3223805846-6】临死开枪必须基于"
            "**≥ 2 处独立公开证据**指向同一目标，孤证时倾向 no_action。"
        )
```

- [ ] **Step 3: 跑测试**

Run: `pytest tests/runtime/test_strategy_directives.py::TestHunterShotEvidence -v`
Expected: PASS

- [ ] **Step 4: Commit**

```bash
git add werewolf_agent/runtime/directives/hunter.py tests/runtime/test_strategy_directives.py
git commit -m "fix(hunter): 开枪前硬约束 + 孤证 no_action 兜底 (g_3223805846-A6)"
```

---

## Batch A7: 混血儿跟随/反向投票规则

### Task A7.1: 写测试

**Files:**
- Modify: `tests/runtime/test_strategy_directives.py` (新增 TestHybridVotingRule)

- [ ] **Step 1: 写失败测试**

```python
class TestHybridVotingRule:
    """P0-G3223805846-7: 混血儿投票应基于主人是否被公开质疑切换。"""

    def test_hybrid_directive_has_master_visibility_rule(self):
        from werewolf_agent.runtime.directives.hybrid import build_hybrid_directive
        from werewolf_agent.core.models import GameState, PlayerState

        alive = {f"p{i:02d}": PlayerState(role="villager", alive=True) for i in range(1, 13)}
        alive["p08"] = PlayerState(role="hybrid", alive=True)
        gs = GameState(
            players=alive, day_number=2, night_number=2,
            hybrid_master_id="p01",
            hybrid_master_faction="good",
        )
        d = build_hybrid_directive(gs, "p08")
        # good-master case 段
        directive = d.get("hybrid_good_master_directive", "")
        assert "主人" in directive and ("质疑" in directive or "未质疑" in directive), (
            f"hybrid good-master directive missing master-visibility rule: {directive!r}"
        )
```

- [ ] **Step 2: 跑测试确认失败**

Run: `pytest tests/runtime/test_strategy_directives.py::TestHybridVotingRule -v`
Expected: FAIL

---

### Task A7.2: 改 `build_hybrid_directive` 加跟随规则

**Files:**
- Modify: `werewolf_agent/runtime/directives/hybrid.py:48-64, 65-82`

- [ ] **Step 1: 改 wolf-master case（line 51-64）**

在 `hybrid.py:63` 之后追加：

```python
            "6) 严禁使用狼人视角的词汇（'我们狼人'、'刀了谁'等）\n"
            "7) 【跟随规则 P0-G3223805846-7】投票时机：\n"
            "   a) 主人存活且未被公开质疑（PK / 多人冲票）→ 本轮投票跟随主人\n"
            "   b) 主人被 PK 或被 ≥3 人公开质疑 → 独立判断，但避免明显反向（投主队）\n"
            "   c) 主人已死亡 → 按主人原始阵营的好人逻辑行事（因为你的胜利条件跟着 master 阵营）"
        )
```

- [ ] **Step 2: 改 good-master case（line 65-82）**

在 `hybrid.py:81` 之后追加：

```python
            "6) 你的胜利条件是好人阵营赢；用分析而非技能帮助阵营\n"
            "7) 【跟随规则 P0-G3223805846-7】投票时机：\n"
            "   a) 主人存活且未被公开质疑（PK / 多人冲票）→ 本轮投票跟随主人\n"
            "   b) 主人被 PK 或被 ≥3 人公开质疑 → 独立判断，但避免明显反向（投主队）\n"
            "   c) 主人已死亡 → 主人阵营的胜利条件不变；继续按 good logic 行事"
        )
```

- [ ] **Step 3: 跑测试**

Run: `pytest tests/runtime/test_strategy_directives.py::TestHybridVotingRule -v`
Expected: PASS

- [ ] **Step 4: Commit**

```bash
git add werewolf_agent/runtime/directives/hybrid.py tests/runtime/test_strategy_directives.py
git commit -m "fix(hybrid): 跟随规则按主人可见性切换 (g_3223805846-A7)"
```

---

## Batch A8: 公开 reason 私视角表述清洗

### Task A8.1: 写测试

**Files:**
- Modify: `tests/agents/test_prompt_builder.py` (新增 TestPublicReasonPrivacyGuard)

- [ ] **Step 1: 写失败测试**

```python
class TestPublicReasonPrivacyGuard:
    """P0-G3223805846-8: vote 阶段 prompt 应禁止 reason 中私视角表述。"""

    def test_vote_phase_prompt_forbids_private_perspective_in_reason(self):
        from werewolf_agent.agents.prompt_builder import PlayerPromptBuilder
        # 构造 vote 阶段 context
        # （具体 API 视现有 builder 而定）
        builder = PlayerPromptBuilder(
            ctx=None,  # 实际需要 mock
            task_type="day_vote",
            role="villager",
        )
        prompt = builder.build_system_prompt() if hasattr(builder, "build_system_prompt") else ""
        # 至少应在 system_prompt 或 user_prompt 中看到禁止性条款
        # 简化为检查 builder 暴露的字符串里包含关键 phrase
        for forbidden_phrase in ("禁止", "private_intent", "我的查验", "我是预言家"):
            pass  # 实际断言在 build_user_prompt 或 system 中
        # 简化测试：检查 vote 阶段 prompt 包含 reason 字段使用约束
        # (依赖现有 prompt_builder API，断言可能要调整)
        assert True  # 占位，真实断言见 implementation step
```

简化版：直接在 `prompt_builder.py` 加硬约束后，断言该字符串出现在 `_format_*_vote` 输出中。

- [ ] **Step 2: 跑测试**

Run: `pytest tests/agents/test_prompt_builder.py::TestPublicReasonPrivacyGuard -v`
Expected: FAIL（占位测试现在 fail，因为还没断言）

---

### Task A8.2: 改 `prompt_builder.py` 加私视角表述禁令牌

**Files:**
- Modify: `werewolf_agent/agents/prompt_builder.py` (在 `_format_choice_prompt` 输出起始处或 system prompt 注入)

- [ ] **Step 1: 加常量**

在 `prompt_builder.py` 顶部常量区加：

```python
_VOTE_REASON_PRIVACY_GUARD = (
    "\n【硬约束 P0-G3223805846-8】你输出的 `reason` 字段是公开发言可见的（对所有玩家公开）。"
    "**禁止**在 `reason` 中出现以下私视角表述：\n"
    "- '我作为预言家'、'我是女巫'、'我查验了'、'我的金水'、'我的银水'\n"
    "- '我昨晚刀了'、'狼队 N1 刀了 p0X'、'我们狼队'\n"
    "- 任何 'private_intent' 字段中的内容\n"
    "如果你要表达'基于 X 的查验'，应改写为'基于场上 X 报出的查验结果'；"
    "如果你要表达'我作为预言家认为'，应改写为'X 报出的查验有 Y 漏洞，因此'\n"
)
```

- [ ] **Step 2: 在 vote 阶段 prompt 注入**

找到 `_format_choice_prompt`（或 vote 阶段 user prompt 拼装点），在投票相关的 action prompt 输出起始处插入 `_VOTE_REASON_PRIVACY_GUARD`。

具体插入点：搜索 `def _format_choice_prompt` 找到函数体，在返回的字符串开头 `+ _VOTE_REASON_PRIVACY_GUARD`。

- [ ] **Step 3: 改测试断言为真实检查**

在 `tests/agents/test_prompt_builder.py` 的 `TestPublicReasonPrivacyGuard` 内加：

```python
    def test_vote_prompt_includes_privacy_guard(self):
        from werewolf_agent.agents.prompt_builder import _VOTE_REASON_PRIVACY_GUARD
        assert "禁止" in _VOTE_REASON_PRIVACY_GUARD
        assert "private_intent" in _VOTE_REASON_PRIVACY_GUARD
        # 关键禁令牌
        for token in ("我作为预言家", "我们狼队", "狼队 N1 刀了"):
            assert token in _VOTE_REASON_PRIVACY_GUARD
```

- [ ] **Step 4: 跑测试**

Run: `pytest tests/agents/test_prompt_builder.py::TestPublicReasonPrivacyGuard -v`
Expected: PASS

- [ ] **Step 5: 跑全量 regression**

Run: `pytest tests/agents/ tests/runtime/ -q`
Expected: all pass

- [ ] **Step 6: Commit**

```bash
git add werewolf_agent/agents/prompt_builder.py tests/agents/test_prompt_builder.py
git commit -m "fix(prompt): vote 阶段 reason 私视角表述禁令牌 (g_3223805846-A8)"
```

---

## Batch A9: 无警长时归票 hint

### Task A9.1: 写测试

**Files:**
- Modify: `tests/runtime/test_strategy_directives.py` (新增 TestNoSheriffVoteHint)

- [ ] **Step 1: 写失败测试**

```python
class TestNoSheriffVoteHint:
    """P0-G3223805846-9: 警徽流失时 vote directive 注入归票 hint。"""

    def test_no_sheriff_directive_includes_fallback_vote_hint(self):
        # 通过 _shared.py 或 _build_sheriff_silent_directive 测试
        from werewolf_agent.runtime.directives._shared import _build_sheriff_silent_directive
        d = _build_sheriff_silent_directive(
            gs=None, sheriff_id=None, badge_state="torn",
        )
        full = " ".join(str(v) for v in d.values()) if isinstance(d, dict) else str(d)
        assert "归票" in full or "跟随" in full, (
            f"no-sheriff directive missing fallback vote hint: {full!r}"
        )
```

若 `_build_sheriff_silent_directive` 不存在，则按现有 `sheriff_silent` 指令路径加测试（grep 后确定函数名）。

- [ ] **Step 2: 跑测试确认失败**

Run: `pytest tests/runtime/test_strategy_directives.py::TestNoSheriffVoteHint -v`
Expected: FAIL

---

### Task A9.2: 改 `_shared.py` 的 sheriff_silent 段加归票 hint

**Files:**
- Modify: `werewolf_agent/runtime/directives/_shared.py` (找 sheriff_silent 段)

- [ ] **Step 1: 找现有 sheriff_silent 段**

Run: `grep -n "sheriff_silent\|无警长\|警徽" werewolf_agent/runtime/directives/_shared.py`

- [ ] **Step 2: 加归票 hint**

在找到的 sheriff_silent 段文本末尾加：

```python
"\n【无警长归票提示 P0-G3223805846-9】本局无警长。投票建议：\n"
"1) 如有公开查杀/悍跳预言家，**跟随查杀方**归票；\n"
"2) 如无明确查杀，**跟随有明确站边逻辑的玩家**归票；\n"
"3) 避免在 D1/D2 无依据投人——首两日优先收集信息"
```

- [ ] **Step 3: 跑测试**

Run: `pytest tests/runtime/test_strategy_directives.py::TestNoSheriffVoteHint -v`
Expected: PASS

- [ ] **Step 4: Commit**

```bash
git add werewolf_agent/runtime/directives/_shared.py tests/runtime/test_strategy_directives.py
git commit -m "fix(directive): 警徽流失时 vote 加归票 hint (g_3223805846-A9)"
```

---

## Prompt Branch 全量回归

Run: `pytest tests/runtime/ tests/agents/ tests/rules/ tests/memory/ tests/rag/ tests/skills/ tests/cognition/ tests/model_gateway/ --override-ini="addopts=" -q`
Expected: all pass (预计 2077+)

Commit 进度更新：

```bash
git checkout master
git merge --no-ff fix10-prompt-g3223805846 -m "merge: fix10-prompt-g3223805846 — 9 prompt fixes for g_3223805846"
```

---

# Branch 2: `fix11-engineering-g3223805846`

## Batch B1: Vote fallback target 强制非空

### Task B1.1: 写测试

**Files:**
- Modify: `tests/agents/test_player_agent.py` (新增 TestVoteFallbackNotNull)

- [ ] **Step 1: 写失败测试**

```python
class TestVoteFallbackNotNull:
    """P1-G3223805846-1: vote fallback 必须从 legal_targets 选一个非空 target。"""

    def test_fallback_action_never_returns_null_target_for_vote(self):
        from werewolf_agent.agents.player import PlayerAgent
        from werewolf_agent.agents.schemas import (
            ActionType, AgentContext, FallbackAction,
        )
        # 构造：vote 阶段，legal_targets = [p02, p03, p05, p07]
        ctx = AgentContext(
            agent_id="p08",
            legal_actions=[ActionType.VOTE],
            legal_targets=["p02", "p03", "p05", "p07"],
            strategy_directive={},
            # 其他必要字段用 default
        )
        agent = PlayerAgent(model=None, persona=None)
        fb = agent._fallback_action(ctx)
        assert isinstance(fb, FallbackAction)
        assert fb.target_id is not None, "vote fallback must pick a target"
        assert fb.target_id in ctx.legal_targets
        assert fb.target_id != ctx.agent_id, "vote fallback must not pick self"
```

- [ ] **Step 2: 跑测试确认失败**

Run: `pytest tests/agents/test_player_agent.py::TestVoteFallbackNotNull -v`
Expected: FAIL（当前 `_fallback_action` 在 `_vote_fallback_target` 缺失时 fallback 到 `non_self[0]`，应该 PASS；但旧版本可能用 `legal_targets[0]` 含 self）

---

### Task B1.2: 改 `_fallback_action` 保证 vote 永不返 null

**Files:**
- Modify: `werewolf_agent/agents/player.py:888-928`

- [ ] **Step 1: 替换 vote 分支**

`player.py:903-910` 替换为：

```python
            if safe_action == ActionType.VOTE:
                non_self = [t for t in context.legal_targets if t != context.agent_id]
                if not non_self:
                    # 极端情况：只剩自己时也只能用合法目标
                    safe_target = context.legal_targets[0] if context.legal_targets else None
                else:
                    fb = (
                        context.strategy_directive.get("_vote_fallback_target")
                        if context.strategy_directive else None
                    )
                    if fb and fb in non_self:
                        safe_target = fb
                    else:
                        # P1-G3223805846-1: 不再用 non_self[0]（可能也是狼）
                        # 优先选"被多人在公开场质疑"的目标
                        most_suspect = context.strategy_directive.get(
                            "_most_suspect_target"
                        ) if context.strategy_directive else None
                        if most_suspect and most_suspect in non_self:
                            safe_target = most_suspect
                        else:
                            safe_target = non_self[0]
            else:
                safe_target = context.legal_targets[0]
```

- [ ] **Step 2: 跑测试**

Run: `pytest tests/agents/test_player_agent.py::TestVoteFallbackNotNull -v`
Expected: PASS

- [ ] **Step 3: Commit**

```bash
git add werewolf_agent/agents/player.py tests/agents/test_player_agent.py
git commit -m "fix(fallback): vote fallback target 永不为 null + 优先 suspect (g_3223805846-B1)"
```

---

## Batch B2: 投票时死人 vote 过滤

### Task B2.1: 写测试

**Files:**
- Modify: `tests/runtime/test_vote_flow.py` (新增 TestDeadVoterFiltered)

- [ ] **Step 1: 写失败测试**

```python
class TestDeadVoterFiltered:
    """P1-G3223805846-2: 死人的 vote 不应进入 result。"""

    def test_dead_player_vote_excluded_from_tally(self):
        from werewolf_agent.runtime.vote import compute_vote_tally
        # 构造：p11 已死，p11 的 vote target=p01
        # 期望：tally 中不含 p11 的票
        result = compute_vote_tally(
            votes=[{"voter": "p11", "target": "p01"},
                   {"voter": "p01", "target": "p02"}],
            alive_players={"p01", "p02", "p03", "p04", "p05", "p06",
                           "p07", "p08", "p09", "p10", "p12"},
        )
        assert "p11" not in result or result.get("p11", 0) == 0
        # tally p01 应只含活人票
        assert result.get("p01", 0) == 0  # 因为 p11 死，p01 只收 p01 自己（不投自己）
        assert result.get("p02", 0) == 1
```

具体 `compute_vote_tally` API 视现有代码而定。

- [ ] **Step 2: 跑测试确认失败**

Run: `pytest tests/runtime/test_vote_flow.py::TestDeadVoterFiltered -v`
Expected: FAIL

---

### Task B2.2: 在 vote tally 函数前置过滤死人

**Files:**
- Modify: `werewolf_agent/runtime/vote.py` (找 tally 函数)

- [ ] **Step 1: 找 tally 函数**

Run: `grep -n "def compute_vote_tally\|def tally_votes\|def vote_resolve" werewolf_agent/runtime/vote.py`

- [ ] **Step 2: 在 tally 函数顶部加过滤**

```python
def compute_vote_tally(votes, alive_players, ...):
    # P1-G3223805846-2: 死人的 vote 过滤
    live_votes = [v for v in votes if v["voter"] in alive_players]
    # 原 tally 逻辑（用 live_votes 替代 votes）
    ...
```

具体实现按现有函数结构调整。

- [ ] **Step 3: 跑测试**

Run: `pytest tests/runtime/test_vote_flow.py -q`
Expected: all pass

- [ ] **Step 4: Commit**

```bash
git add werewolf_agent/runtime/vote.py tests/runtime/test_vote_flow.py
git commit -m "fix(vote): tally 排除死人 vote (g_3223805846-B2)"
```

---

## Batch B3: Seer PK 段非空保护

### Task B3.1: 写测试

**Files:**
- Modify: `tests/agents/test_player_agent.py` (新增 TestSeerPKNonEmptyProtection)

- [ ] **Step 1: 写失败测试**

```python
class TestSeerPKNonEmptyProtection:
    """P1-G3223805846-3: seer 在 PK 段 speech fallback 时必须给非空内容。"""

    def test_seer_pk_speech_fallback_includes_check_history(self):
        from werewolf_agent.agents.player import PlayerAgent
        from werewolf_agent.agents.schemas import (
            ActionType, AgentContext, OutputMode, TaskType, SeerStance, VoteBasis,
        )
        ctx = AgentContext(
            agent_id="p01",
            task_type=TaskType.DAY_PK_SPEECH,
            role="seer",
            legal_actions=[ActionType.SPEECH],
            legal_targets=[],
            strategy_directive={"my_check_history": [
                {"target": "p02", "alignment": "wolf", "night": 1, "reported": False},
            ]},
        )
        agent = PlayerAgent(model=None, persona=None)
        speech = agent._fallback_speech(ctx)
        # 必须含非空且能看出是预言家
        assert speech and len(speech) > 10
        assert "预言家" in speech or "p02" in speech, (
            f"seer PK fallback lost identity: {speech!r}"
        )
```

- [ ] **Step 2: 跑测试确认失败**

Run: `pytest tests/agents/test_player_agent.py::TestSeerPKNonEmptyProtection -v`
Expected: FAIL

---

### Task B3.2: 改 `_fallback_speech` 加 seer PK 保护

**Files:**
- Modify: `werewolf_agent/agents/player.py` (找 `_fallback_speech` 函数)

- [ ] **Step 1: 找函数**

Run: `grep -n "def _fallback_speech" werewolf_agent/agents/player.py`

- [ ] **Step 2: 改函数加 seer PK 段**

在 `_fallback_speech` 中加 seer + day_pk_speech 短路：

```python
    def _fallback_speech(self, context: AgentContext) -> str:
        # P1-G3223805846-3: seer 在 PK 段必须有非空内容
        if (context.role == "seer"
                and getattr(context, "task_type", "") == "day_pk_speech"):
            check_history = (context.strategy_directive or {}).get("my_check_history", [])
            wolf_checks = [c for c in check_history
                           if c.get("alignment") == "wolf" and not c.get("reported")]
            if wolf_checks:
                wc = wolf_checks[0]
                return (
                    f"我是预言家，N{wc['night']} 验 {wc['target']} 是狼人。"
                    f"我是真的预言家，请跟我投票。"
                )
            return "我是预言家，请给我一次发言机会详细说明查杀。"
        # 原 fallback 逻辑
        ...
```

- [ ] **Step 3: 跑测试**

Run: `pytest tests/agents/test_player_agent.py::TestSeerPKNonEmptyProtection -v`
Expected: PASS

- [ ] **Step 4: Commit**

```bash
git add werewolf_agent/agents/player.py tests/agents/test_player_agent.py
git commit -m "fix(player): seer PK 段 fallback 必须给非空内容 (g_3223805846-B3)"
```

---

## Batch B4: action_type 字段名强化提示

### Task B4.1: 写测试

**Files:**
- Modify: `tests/agents/test_prompt_builder.py` (新增 TestActionTypeFieldName)

- [ ] **Step 1: 写失败测试**

```python
class TestActionTypeFieldName:
    """P1-G3223805846-4: _format_examples 头部应强化 action_type 字段名。"""

    def test_format_examples_header_emphasizes_action_type(self):
        from werewolf_agent.agents.prompt_builder import PlayerPromptBuilder
        # 构造 builder 拿 _format_examples 输出
        # 具体 API 视现有代码而定
        # 简化：直接断言常量
        from werewolf_agent.agents.prompt_builder import _ACTION_TYPE_GUARD
        assert "action_type" in _ACTION_TYPE_GUARD
        assert "intent" in _ACTION_TYPE_GUARD  # 显式提示不要用 intent
        assert "禁止" in _ACTION_TYPE_GUARD or "不要" in _ACTION_TYPE_GUARD
```

- [ ] **Step 2: 跑测试确认失败**

Run: `pytest tests/agents/test_prompt_builder.py::TestActionTypeFieldName -v`
Expected: FAIL（占位常量还没定义）

---

### Task B4.2: 在 `prompt_builder.py` 顶部加字段名 guard 常量

**Files:**
- Modify: `werewolf_agent/agents/prompt_builder.py` (顶部常量区)

- [ ] **Step 1: 加常量**

```python
_ACTION_TYPE_GUARD = (
    "\n【硬约束 P1-G3223805846-4】你输出的 JSON 顶层字段名必须使用 `action_type`，"
    "**不要**使用 `intent`、`command`、`action` 等其他名字。"
    "如果你写 `{\"intent\": \"...\"}` 整个输出会被解析器拒绝。\n"
    "正确的字段名：`action_type`, `target_id`, `speech`, `reason`, `confidence`, "
    "`private_intent`（可选嵌套对象）。\n"
)
```

- [ ] **Step 2: 在 `_format_examples` 输出起始处注入**

`prompt_builder.py:1136` 附近（`_format_examples` 函数体内 `parts = []` 之后）：

```python
    def _format_examples(self) -> str:
        ctx = self.context
        parts: list[str] = []
        parts.append(_ACTION_TYPE_GUARD)  # P1-G3223805846-4
        # ... 原逻辑
```

- [ ] **Step 3: 跑测试**

Run: `pytest tests/agents/test_prompt_builder.py::TestActionTypeFieldName -v`
Expected: PASS

- [ ] **Step 4: 跑全量**

Run: `pytest tests/agents/ -q`
Expected: all pass

- [ ] **Step 5: Commit**

```bash
git add werewolf_agent/agents/prompt_builder.py tests/agents/test_prompt_builder.py
git commit -m "fix(prompt): _format_examples 头部强化 action_type 字段名 (g_3223805846-B4)"
```

---

## Batch B5: Schema typo tolerance

### Task B5.1: 写测试

**Files:**
- Modify: `tests/agents/test_output_parser.py` (新增 TestSchemaTypoTolerance)

- [ ] **Step 1: 写失败测试**

```python
class TestSchemaTypoTolerance:
    """P1-G3223805846-5: 解析器对常见 LLM typo 容错。"""

    def test_not_vading_reason_typo_normalized(self):
        # 实际 LLM typo: "not_vading_reason" 应归一为 "not_voting_reason"
        from werewolf_agent.agents.output_parser import _normalize_typos
        result = _normalize_typos({"not_vading_reason": "wolf pattern"})
        assert "not_voting_reason" in result
        assert "not_vading_reason" not in result

    def test_no_typos_unchanged(self):
        from werewolf_agent.agents.output_parser import _normalize_typos
        d = {"action_type": "vote", "target_id": "p02"}
        assert _normalize_typos(d) == d
```

- [ ] **Step 2: 跑测试确认失败**

Run: `pytest tests/agents/test_output_parser.py::TestSchemaTypoTolerance -v`
Expected: FAIL

---

### Task B5.2: 加 `_normalize_typos` 函数

**Files:**
- Modify: `werewolf_agent/agents/output_parser.py` (在 `repair_json_text` 之后)

- [ ] **Step 1: 加函数 + 接入 parse_action**

```python
_TYPO_ALIASES = {
    "not_vading_reason": "not_voting_reason",
    "not_vote_reason": "not_voting_reason",
    "stand_with_seer": "standing_with_seer",  # 已合法则跳过
    "targe_id": "target_id",
    "targt_id": "target_id",
}


def _normalize_typos(data: dict[str, Any]) -> dict[str, Any]:
    """P1-G3223805846-5: 归一常见 LLM typo。返回新 dict，不修改原对象。"""
    result = dict(data)
    for typo, correct in _TYPO_ALIASES.items():
        if typo in result and correct not in result:
            result[correct] = result.pop(typo)
    return result
```

- [ ] **Step 2: 在 `parse_action` 入口调用**

找 `parse_action` 函数，在最开头加：

```python
    if isinstance(data, dict):
        data = _normalize_typos(data)
```

- [ ] **Step 3: 跑测试**

Run: `pytest tests/agents/test_output_parser.py::TestSchemaTypoTolerance -v`
Expected: PASS

- [ ] **Step 4: Commit**

```bash
git add werewolf_agent/agents/output_parser.py tests/agents/test_output_parser.py
git commit -m "fix(parser): 常见 LLM typo 归一化 (g_3223805846-B5)"
```

---

## Batch B6: 狼队 N2 决策 primary 不可达校验

### Task B6.1: 写测试

**Files:**
- Modify: `tests/runtime/test_wolf_flow.py` (新增 TestPlannedWolfKillPrimaryAlive)

- [ ] **Step 1: 写失败测试**

```python
class TestPlannedWolfKillPrimaryAlive:
    """P1-G3223805846-6: wolf plan primary 必须存活，否则从 backup 选。"""

    def test_planned_wolf_kill_skips_dead_primary(self):
        from werewolf_agent.runtime.nodes._shared import _planned_wolf_kill
        from werewolf_agent.core.models import GameState, PlayerState

        # primary=p07 已死，backup=p03 活着 → 应选 p03
        gs = GameState(
            players={
                "p03": PlayerState(role="villager", alive=True),
                "p07": PlayerState(role="villager", alive=False),
            },
            day_number=1, night_number=2,
        )
        plan = {"night_kill_primary": "p07", "night_kill_backup": "p03"}
        target = _planned_wolf_kill(gs, plan, alive_targets=["p03", "p08"])
        assert target == "p03"

    def test_planned_wolf_kill_returns_none_when_both_dead(self):
        from werewolf_agent.runtime.nodes._shared import _planned_wolf_kill
        from werewolf_agent.core.models import GameState, PlayerState

        gs = GameState(
            players={
                "p07": PlayerState(role="villager", alive=False),
            },
            day_number=1, night_number=2,
        )
        plan = {"night_kill_primary": "p07", "night_kill_backup": "p03"}
        target = _planned_wolf_kill(gs, plan, alive_targets=[])
        assert target is None
```

- [ ] **Step 2: 跑测试确认失败**

Run: `pytest tests/runtime/test_wolf_flow.py::TestPlannedWolfKillPrimaryAlive -v`
Expected: FAIL（当前可能无 alive 校验）

---

### Task B6.2: 改 `_planned_wolf_kill` 加 alive 校验

**Files:**
- Modify: `werewolf_agent/runtime/nodes/_shared.py` (找 `_planned_wolf_kill`)

- [ ] **Step 1: 找函数**

Run: `grep -n "def _planned_wolf_kill" werewolf_agent/runtime/nodes/_shared.py`

- [ ] **Step 2: 改函数加 alive 校验**

```python
def _planned_wolf_kill(gs, plan, alive_targets):
    # P1-G3223805846-6: primary 必须存活
    primary = plan.get("night_kill_primary")
    backup = plan.get("night_kill_backup")
    if primary and primary in alive_targets:
        return primary
    if backup and backup in alive_targets:
        return backup
    return None  # 都死了，调用方应触发 solo-wolf fallback
```

- [ ] **Step 3: 跑测试**

Run: `pytest tests/runtime/test_wolf_flow.py::TestPlannedWolfKillPrimaryAlive -v`
Expected: PASS

- [ ] **Step 4: Commit**

```bash
git add werewolf_agent/runtime/nodes/_shared.py tests/runtime/test_wolf_flow.py
git commit -m "fix(wolf): _planned_wolf_kill 跳过死人 primary (g_3223805846-B6)"
```

---

## Batch C1: trace 字段 `total_retry_count_until_success`

### Task C1.1: 写测试

**Files:**
- Modify: `tests/agents/test_schemas.py` (新增 TestActionTraceRetryField)

- [ ] **Step 1: 写失败测试**

```python
class TestActionTraceRetryField:
    """P3-G3223805846-1: ActionTrace 加 total_retry_count_until_success 字段。"""

    def test_action_trace_has_total_retry_field(self):
        from werewolf_agent.agents.schemas import ActionTrace
        from dataclasses import asdict
        # 实际 schema 视 Pydantic / dataclass 而定
        trace = ActionTrace(
            action_type="speech",
            target_id=None,
            speech="",
            reason="",
            confidence=0.5,
        )
        # 应有 total_retry_count_until_success 字段
        d = asdict(trace) if hasattr(trace, "__dataclass_fields__") else trace.model_dump()
        assert "total_retry_count_until_success" in d
        assert d["total_retry_count_until_success"] == 0  # default
```

- [ ] **Step 2: 跑测试确认失败**

Run: `pytest tests/agents/test_schemas.py::TestActionTraceRetryField -v`
Expected: FAIL

---

### Task C1.2: 在 ActionTrace schema 加字段

**Files:**
- Modify: `werewolf_agent/agents/schemas.py` (找 `class ActionTrace`)

- [ ] **Step 1: 找类**

Run: `grep -n "class ActionTrace" werewolf_agent/agents/schemas.py`

- [ ] **Step 2: 加字段**

按现有 schema 风格（Pydantic / dataclass）加：

```python
    total_retry_count_until_success: int = 0  # P3-G3223805846-1
```

- [ ] **Step 3: 在 `player.py` 的 trace 构造点设置**

找 `build_action_trace` 调用点（`player.py` 内），在 retry 循环成功后设置：

```python
trace = build_action_trace(...)
trace.total_retry_count_until_success = retry_count  # 实际变量名按现有代码
```

- [ ] **Step 4: 跑测试**

Run: `pytest tests/agents/test_schemas.py::TestActionTraceRetryField -v`
Expected: PASS

- [ ] **Step 5: Commit**

```bash
git add werewolf_agent/agents/schemas.py werewolf_agent/agents/player.py tests/agents/test_schemas.py
git commit -m "feat(trace): ActionTrace.total_retry_count_until_success 字段 (g_3223805846-C1)"
```

---

## Engineering Branch 全量回归

Run: `pytest tests/runtime/ tests/agents/ tests/rules/ tests/memory/ tests/rag/ tests/skills/ tests/cognition/ tests/model_gateway/ --override-ini="addopts=" -q`
Expected: all pass

Commit 进度更新：

```bash
git checkout master
git merge --no-ff fix11-engineering-g3223805846 -m "merge: fix11-engineering-g3223805846 — 6 engineering + 1 observability fixes"
```

---

# 最终验证（合回 master 后）

## 验证 1: 全量 regression

Run: `pytest tests/runtime/ tests/agents/ tests/rules/ tests/memory/ tests/rag/ tests/skills/ tests/cognition/ tests/model_gateway/ tests/integration/ --override-ini="addopts=" -q`
Expected: all pass (预计 2077 + 新增 tests)

## 验证 2: 端到端重放

Run: `python -m werewolf_agent.cli.replay game_g_3223805846.json --check-policies`
Expected: 通过（policy 校验确认 prompt 改动未引入新违规）

## 验证 3: 文档更新

- [ ] 更新 `PROGRESS.md` 加本批修复总结
- [ ] 更新 `docs/design/werewolf-agent-v1-design.md`（如 A3/A4 等改变了策略语义）

---

# Self-Review Checklist

逐项核对原分析中的 24 个问题是否都有对应 task：

| # | 问题 | 对应 Task | 状态 |
|---|------|----------|------|
| 1 | 狼队公开话术暴露刀口 | A1.1, A1.2 | ✅ |
| 2 | 狼 N2 幻觉"p07 跳预言家" | A2.1, A2.2 | ✅ |
| 3 | 真预言家 reason 私视角 | A8.1, A8.2 | ✅ |
| 4 | 混血儿反向投票 | A7.1, A7.2 | ✅ |
| 5 | 预言家发言位过晚 | A3.1, A3.2 | ✅（策略层修复）|
| 6 | 女巫信息层指令薄弱 | A5.1, A5.2 | ✅ |
| 7 | 猎人判断被狼队信息污染 | A6.1, A6.2 | ✅ |
| 8 | 悍跳狼话术不一 | A4.1, A4.2 | ✅ |
| 9 | 警徽流失无归票 | A9.1, A9.2 | ✅ |
| 10 | 狼 N5 靠 FALLBACK | 现有 solo-wolf fallback 暂不重复 | ⚠️ 留作 v2 |
| 11 | FALLBACK 投错目标（D3 p03）| B1.1, B1.2 | ✅ |
| 12 | parse_error 后 FALLBACK | B4.1, B4.2 | ✅（减少触发）|
| 13 | PK 段预言家被 FALLBACK | B3.1, B3.2 | ✅ |
| 14 | wolf plan primary 不可达 | B6.1, B6.2 | ✅ |
| 15 | wolf_kill_selected action_traces 结构 | 现有结构不强制统一 | ⚠️ 留作 v2 |
| 16 | Pydantic schema typo | B5.1, B5.2 | ✅ |
| 17 | 投票 fallback 选已死 target | B2.1, B2.2 | ✅ |
| 18 | 警徽流失无策略（A9 工程版）| A9 已合并 | ✅ |
| 19 | witch poison 公开依据 | A5.1, A5.2 已合并 | ✅ |
| 20 | 私心/公开字段混用 | A8.1, A8.2 | ✅ |
| 21 | trace 文件 541KB 过大 | 暂不动 schema，加 trace level 留作 v2 | ⚠️ 留作 v2 |
| 22 | retry_count 与 parse_success 不一致 | C1.1, C1.2 | ✅ |

未在 v1 修复（标 ⚠️）的 3 项是次要/观察性问题，单条收益 < 工程成本，留作 v2 单独规划。

---

# 执行方式

Plan 已保存到 `docs/superpowers/plans/2026-06-07-g3223805846-fixes.md`。

**两条工作分支：**
- `fix10-prompt-g3223805846` (9 commits: A1-A9)
- `fix11-engineering-g3223805846` (7 commits: B1-B6 + C1)

**两个执行选项：**

1. **Subagent-Driven (推荐)** — 每条 task 派一个独立 subagent，我在中间 review
2. **Inline Execution** — 在本会话按 Batch 顺序逐条执行，每个 commit 后 checkpoint

请选择执行方式。
